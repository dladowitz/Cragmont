%nav.club-subnav{aria: {label: "About the club"}}
  = link_to "About", about_path, aria: {current: ("page" if current_page?(about_path))}
  = link_to "Membership", membership_path, aria: {current: ("page" if current_page?(membership_path))}
  = link_to "History", history_path, aria: {current: ("page" if current_page?(history_path))}
  = link_to "Join the List", join_the_list_path, aria: {current: ("page" if current_page?(join_the_list_path))}
