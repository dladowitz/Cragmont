- if record.errors.any?
  .form-errors
    %h2
      - if record.errors.one?
        Almost there - please fix the item below
      - else
        Almost there - please fix the items below
    %ul
      - record.errors.full_messages.each do |message|
        %li= message
