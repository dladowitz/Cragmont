= render "admin/shared/header", title: "Edit Reimbursement"
%section.panel
  .panel-header
    %div
      %h2 Edit reimbursement
      %p.muted= @trip.name
  = render "form", trip: @trip, reimbursement: @reimbursement
