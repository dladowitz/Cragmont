= render "admin/shared/header", title: "New Reimbursement"
%section.panel
  .panel-header
    %div
      %h2 Record reimbursement
      %p.muted= @trip.name
  = render "form", trip: @trip, reimbursement: @reimbursement
