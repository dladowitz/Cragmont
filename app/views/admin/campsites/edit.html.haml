= render "admin/shared/header", title: "Edit campsite"
%section.panel.campsite-form-panel
  .panel-header
    %div
      .trip-title-line
        %h2= @trip.name
        %p= @trip.location
  = render "form", trip: @trip, campsite: @campsite, campgrounds: @campgrounds
