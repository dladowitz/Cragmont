= render "admin/shared/header", title: "Add campsite"
%section.panel
  .panel-header
    %div
      %h2= @trip.name
      %p= @trip.location
  = render "form", trip: @trip, campsite: @campsite, campgrounds: @campgrounds
