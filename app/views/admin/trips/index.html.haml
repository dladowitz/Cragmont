= render "admin/shared/header", title: "Trips"
%section.panel
  .panel-header
    %div
      %h2 Trip inventory
      %p Manage club trips and the campsites reserved for each trip.
    = link_to "New trip", new_admin_trip_path, class: "button"
  = render "table", trips: @trips
